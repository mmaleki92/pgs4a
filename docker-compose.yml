services:
  pgs4a:
    build: .
    stdin_open: true
    tty: true
    volumes:
      # Mount your game source directory
      - ./examples:/opt/pgs4a/examples
      # Mount output directory for built APKs
      - ./output:/output
